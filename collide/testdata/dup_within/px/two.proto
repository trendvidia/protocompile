syntax = "proto3";

package px;

// px.Dup again. The two files do not import each other; protoregistry
// panics on the second one, and the compiler rejects the pair when both
// are roots of one compile.
message Dup {
  string y = 1;
}
