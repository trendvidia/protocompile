syntax = "proto3";

package px;

// px.Dup again. The two files do not import each other, so nothing in the
// module's own build rejects this; protoregistry panics on the second one.
message Dup {
  string y = 1;
}
