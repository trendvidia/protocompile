name: windows
# Runs on a self-hosted Windows runner (protocompile-omen), so the
# GitHub-hosted cost that once justified the narrow trigger no longer
# applies. The triggers are unchanged for now — release events, tag pushes,
# and manual dispatch — because broadening them is a separate decision;
# see #197.
#
# This workflow was disabled_manually from 2026-07-17 to 2026-09-03 after
# three red runs, while both this file and CONTRIBUTING.md continued to
# describe it as gating releases. Five releases shipped in that window.
on:
  release:
    types: [published]
  push:
    tags: ['v*']
  workflow_dispatch: {} # manual runs via `gh workflow run`
permissions:
  contents: read
jobs:
  windows:
    name: windows (go:${{ matrix.go-version.name }})
    env:
      PROTOC_ARTIFACT_SUFFIX: win64
      PATH_SEP: ;
      # Scoped to this job rather than `git config --global`. The runner
      # service runs as NT AUTHORITY\NETWORK SERVICE, whose global git
      # config is shared with the eight other repositories' runners on this
      # machine — writing core.autocrlf there would reach all of them.
      GIT_CONFIG_COUNT: "2"
      GIT_CONFIG_KEY_0: core.autocrlf
      GIT_CONFIG_VALUE_0: "false"
      GIT_CONFIG_KEY_1: core.eol
      GIT_CONFIG_VALUE_1: lf
    runs-on: [self-hosted, Windows, X64]
    strategy:
      matrix:
        go-version:
          - name: latest
            version: 1.26.x
          - name: previous
            version: 1.25.x
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ matrix.go-version.version }}
      - shell: bash
        run: make protoc && go test ./...
