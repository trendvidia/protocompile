syntax = "proto3";

package test;

// Each rejected argument shape. The legalize pass should emit one
// "shape is not allowed" diagnostic per offending argument.

@list_arg([1, 2, 3])
message M1 {}

@dict_arg({key: "value"})
message M2 {}

@negated_path(-foo.bar)
message M3 {}
